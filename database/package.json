{
  "name": "quartziq-database",
  "version": "1.0.0",
  "description": "QuartzIQ Database Layer",
  "scripts": {
    "migrate": "node migrate.js",
    "migrate:rollback": "node migrate.js rollback",
    "seed": "node seed.js",
    "test-connection": "node test-connection.js"
  },
  "dependencies": {
    "pg": "^8.11.0",
    "dotenv": "^16.3.0"
  },
  "devDependencies": {
    "@types/pg": "^8.10.0"
  }
}
